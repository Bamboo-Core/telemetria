# Placeholder embutido na imagem. Em produção é SOBRESCRITO pelo bind-mount de
# ../telegraf-huawei.conf definido no docker-compose.yml. Não edite aqui.
[agent]
  interval = "10s"
  flush_interval = "10s"

[[inputs.huawei_telemetry_dialout]]
  service_address = ":57400"
  data_format = "json"
  transport = "grpc"
